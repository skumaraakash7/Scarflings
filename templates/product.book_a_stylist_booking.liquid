{% include 'product-content.zipifypages', contentprt: 'header' %}
<!--<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.4/jquery.min.js"></script>-->
<!--<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>-->
{{ '//fonts.googleapis.com/css?family=DM+Serif+Text:400,400i|Poppins:300,400,500,600,700&display=swap' | stylesheet_tag }}
{{ '//ajax.googleapis.com/ajax/libs/jqueryui/1.11.4/themes/smoothness/jquery-ui.css' | stylesheet_tag }}
{% assign free_shipping_threshold = all_products["free-shipping"].compare_at_price %}
{%assign cartlist = ''%}
{% for lineitem in cart.items %}
  {% assign linestr = lineitem.variant_id | append: ':'| append: lineitem.quantity %}
  {% assign cartlist = cartlist | append: linestr | append: ',' %}
{% endfor %}

<!--Product Details Area Start-->
{% assign current_variant = product.selected_or_first_available_variant %}
{% assign enable_zoom = true %}
{% assign product_img_size = settings.img_size_product %}
<meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
<meta itemprop="image" content="{{ product.featured_image.src | img_url: 'grande' }}">

<style>
.rondedImageLeft.imgLeft {
	position: relative;
}
.stylistRow .care_text p {
color: #000 !important;
}
/*#form_buy .form-group:first-child {
    display: none;
}*/
h3.book_an_app_h1 {
	margin-bottom: 20px;
}

section p:last-child {
	margin-bottom: 0px !important;
	padding-bottom: 0px;
}

.book_personal_stylist_product_parent form textarea {
	height: 135px;
}

.book_personal_stylist_product_parent form textarea#bta-field-comments_optional {
	height: 185px;
}

.book_personal_stylist_product_parent form .form-group input[type=radio] {
	width: 10px !important;
	margin-left: 8px !important;
}

.stylistRow .slick-slider.slick-vertical {
	display: none;
}

.rondedImageLeft .slick1-dots li {
	display: none !important;
}

.user_card_bg {
	width: 100%;
	background-size: cover;
	background-image: url(//cdn.shopify.com/s/files/1/0070/0835/4356/files/2019-11-15_2.png?v=1573825137);
	background-repeat: no-repeat;
	background-position: center;
	margin-bottom: 145px;
}

h1,
h2,
h3,
h4,
h5,
h6 {
	font-family: 'DM Serif Text', serif !important;
}

section p {
	margin-bottom: 10px !important;
	font-family: 'Poppins', sans-serif !important;
}

section.page_title h1 {
	font-size: 45px;
	font-weight: 400;
}

section.page_title {
	margin-bottom: 105px;
}

.rondedImageLeft .slick-list,
.rondedImageLeft .slick-track,
.rondedImageLeft .slick-slider {
	width: 100% !important;
	overflow: initial;
}

.rondedImageLeft .slick-slide {
	height: 410px;
	width: 100% !important;
}

.SliderLeftImage {
	top: -50px;
	border: 10px solid #fff;
	border-radius: 50%;
	width: 505px;
	z-index: 999;
	left: auto;
	right: 25px;
	position: absolute;
	max-width: 100%;
}

.SliderLeftImage img {
	width: 100%;
	height: 100%;
}

.simpletext {
	padding: 20px 0px 10px 0px;
	height: 100%;
	display: flex;
	justify-content: center;
	flex-direction: column;
}

.wrap-dropdown-content.active-dropdown-content.prodesc {
	display: none;
}

.simpletext h5 {
	color: #fff;
	font-size: 20px;
	text-transform: uppercase;
}

.stylistRow h4 {
	padding-bottom: 7px;
	font-size: 36px;
	color: #fff;
}

.stylistRow p h4 {
	font-size: 36px;
	font-weight: 400;
	line-height: 1em;
	margin-top: 5px;
}

.stylistRow p {
	color: #fff !important;
	width: calc(100% - 65px);
}

.simpletext p {
	font-size: 14px;
	color: #fff;
	font-family: 'Poppins', sans-serif !important;
}

.simpletext h2 {
	color: #b44b2b;
	font-weight: 400;
	font-size: 35px;
	line-height: 1.2em;
	margin-bottom: 15px;
}

body h3 {
	text-transform: unset !important;
	letter-spacing: unset !important;
	font-size: 40px !important;
	font-weight: 400 !important;
	color: #b54f28 !important;
}

.book_personal_stylist_product_parent {
	width: 100%;
	text-align: center;
	position: relative;
}

button {
	font-family: 'DM Serif Text', serif !important;
}

.book_personal_stylist_product_parent p {
	font-size: 17px !important;
	line-height: 1.6em !important;
	color: #000000 !important;
	font-weight: 400 !important;
	margin-bottom: 20px !important;
	width: 835px;
	font-family: 'Poppins', sans-serif !important;
	margin: 0 auto 50px auto !important;
	max-width: 100%;
}

.book_personal_stylist_product_parent form {
	    background: #ffffff;
    box-shadow: 1px 1px 12px 1px #ddd;
    padding: 50px 35px 55px 35px;
    text-align: left;
    position: relative;
    border-radius: 10px;
    z-index: 999;
    display: inline-block;
    width: 100%;
}
.form-group.sesami__buttonWrapper button {
    width: 100%;
    text-align: left;
    background: transparent !important;
    border: 1px solid #c3872b !important;
    color: #888 !important;
    font-family: poppins !important;
    font-size: 17px;
    text-transform: capitalize;
    height: 50px;
    margin-top: 35px;
    border-radius: 5px !important;
  	font-size: 16px !important;
}
.form-group.sesami__buttonWrapper button:hover {
    background: transparent !important;
    border: 1px solid #c3872b !important;
  	height: 50px !important;
}
.book_personal_stylist_product_form .form-group {
    width: 50%;
    float: left;
    padding: 0px 15px;
}
.book_personal_stylist_product_form .form-group:last-child {
    width: 100%;
}
.top_background {
	background-image: url(//cdn.shopify.com/s/files/1/0070/0835/4356/files/6_large.png?v=1573653350);
	text-align: center;
	background-repeat: no-repeat;
	background-position: -134px 0px;
	width: 450px;
	height: 450px;
	position: absolute;
	top: 230px;
	max-width: 100%;
}

.bottom_background {
	background-image: url(//cdn.shopify.com/s/files/1/0070/0835/4356/files/6_large.png?v=1573653350);
	text-align: center;
	background-repeat: no-repeat;
	background-position: calc(100% - -175px) bottom;
	right: 0px;
	width: 450px;
	height: 450px;
	position: absolute;
	bottom: 0px;
	max-width: 100%;
}

.book_personal_stylist_product_parent form label:not(.red_label),
.book_personal_stylist_product_parent form label:not(.error) {
	color: #c3872b;
	font-size: 17px;
	font-family: poppins;
	font-weight: bold;
}

.book_personal_stylist_product_parent form .form-group input,
.book_personal_stylist_product_parent form textarea,
.book_personal_stylist_product_parent form select {
	border: 1px solid #c3872b !important;
	border-radius: 5px !important;
	width: 100% !important;
	font-size: 16px !important;
	color: #848484 !important;
	font-family: poppins !important;
	padding: 10px 12px !important;
	line-height: 1.5em !important;
	resize: none;
  	height:50px !important;
}

.form-group i {
	position: absolute;
	right: 27px;
	bottom: 7px;
	font-size: 36px;
	color: #c3872b;
	pointer-events: none;
}

select {
	/* for Firefox */
	-moz-appearance: none;
	/* for Chrome */
	-webkit-appearance: none;
}


/* For IE10 */

select::-ms-expand {
	display: none;
}

.book_personal_stylist_product_parent form label.red_label,
.book_personal_stylist_product_parent form label[for="bta-field-contact_information"] {
	font-size: 21px;
	color: #b65327;
	font-weight: 500;
	margin-bottom: 10px;
	font-family: 'DM Serif Text', serif !important;
}

.book_personal_stylist_product_parent form .radio:last-child {
	margin-bottom: 0px;
}

.book_personal_stylist_product_parent form .radio label {
	font-weight: 400;
	color: #848484;
	font-size: 16px;
}

.book_personal_stylist_product_parent form .radio {
	margin-bottom: 15px;
	display: flex;
	align-items: center;
}

.checkmark {
	position: absolute;
	background-color: #fff;
	border-radius: 50%;
	width: 17px;
	height: 17px;
	margin-left: 3px;
	margin-top: 3px;
}

.book_personal_stylist_product_parent form .radio input {
	display: inline-block;
	border: 0px !important;
	opacity: 1 !important;
	margin-right: 0px;
	width: auto !important;
	left: 3px;
	top: 3px;
}

.book_personal_stylist_product_parent form .radio input {
	position: absolute;
	opacity: 0;
	cursor: pointer;
	height: 0;
	width: 0;
}

.book_personal_stylist_product_parent form .radio input {
	display: inline-block;
	border: 0px !important;
	opacity: 1 !important;
	margin-right: 8px;
	width: auto !important;
}

.book_personal_stylist_product_parent form p {
	font-size: 16px !important;
	text-align: center;
	padding: 0px 115px;
	margin-bottom: 25px !important;
}

.btn.btn--cta.cpsgts {
	background: #b65327 !important;
	font-size: 18px;
	height: auto;
	border-radius: 10px !important;
	text-align: center;
	padding: 0px;
	margin: 43px auto 0px auto;
	padding: 15px 25px;
	line-height: 1em;
	font-weight: 400;
}

.content_centered {
	width: 630px;
	margin: 0 auto;
	max-width: 100%;
}

.care_row .care_img {
	background: #c38a33;
	border-radius: 50%;
	width: 75px;
	height: 75px;
	text-align: center;
	justify-content: center;
	align-items: center;
	float: left;
	display: flex;
	flex-grow: 0;
	margin-right: 15px;
	flex-shrink: 0;
}

.care_row .care_img img {
	width: 35px;
	height: auto;
}

.care_row {
	display: flex;
	height: 100%;
	align-items: center;
	margin-bottom: 30px;
}

.care_text p {
	font-size: 16px;
	color: #000;
	font-family: 'Poppins', sans-serif !important;
	font-weight: 400;
	margin: 0px;
	line-height: 1.4em;
}

.care_row:last-child {
	margin-bottom: 0px;
}

.booking-form>div:nth-child(1),
.booking-form>div:nth-child(2) {
	width: 50%;
	float: left;
}

.booking-form>div {
	padding: 0px 15px;
	margin-bottom: 50px;
	position: relative;
}

.bta-booking-fields {
	display: inline-block;
	float: unset;
	clear: both;
}

.bta-booking-fields legend {
	font-size: 21px;
	color: #b65327;
	font-weight: 400;
	font-family: 'DM Serif Text', serif !important;
	margin-bottom: 10px;
	padding-top: 0px !important;
}

.book_personal_stylist_product_parent form .bta-booking-field fieldset label {
	font-weight: 400;
	color: #848484;
	font-size: 16px;
	position: relative;
	left: 0px;
	z-index: 999;
	padding-left: 35px;
	margin-bottom: 20px;
	display: block;
	line-height: 1.7em;
}

.radio_circle {
	width: 25px;
	height: 25px;
	border: 1px solid #c3872b;
	border-radius: 50%;
	display: inline-block;
	position: absolute;
	margin-right: 10px;
}

input#bta-field-contact_information {
	display: none;
}

.checkmark {
	position: absolute;
	background-color: #fff;
	border-radius: 50%;
	width: 17px;
	height: 17px;
	margin-left: 3px;
	margin-top: 3px;
	left: 0px;
}

.bta-booking-fields input:checked~.checkmark {
	background-color: #C3872B;
	border-radius: 50%;
	width: 17px;
	height: 17px;
	margin-left: 3px;
	left: 0px;
	margin-top: 3px;
}

.book_personal_stylist_product_parent button {
    margin: 20px auto 0px auto;
    min-width: 180px;
    color: #fff;
    background: #b65327 !important;
    font-size: 21px;
    height: auto;
    border-radius: 10px !important;
    text-align: center;
    padding: 13px 25px;
    line-height: 1em;
    text-shadow: unset;
    box-shadow: none;
    border: 0px !important;
    font-weight: 400;
    text-transform: uppercase;
    width: auto;
}

.book_personal_stylist_product_parent button:hover {
	border: 0px !important;
	height: auto !important;
	box-shadow: none;
	background-image: none !important;
	text-shadow: unset !important;
	opacity: 0.8;
}

.book_personal_stylist_product_parent .btn-addcart-product-detail {
	text-align: center;
}

.book_personal_stylist_product_parent .form-group.comments_input textarea {
	height: 250px;
}

.book_personal_stylist_product_parent .bta-booking-field {
	margin-bottom: 20px;
}

.booking-form .bta-booking-fields {
	margin-bottom: 0px;
}

input[name="properties[The information you provide will not be shared with third parties; it will only be used for the purposes of your styling appointment.]"] {
	display: none;
}

.book_personal_stylist_product_parent .bta-booking-field:last-child label {
	color: #000;
	font-weight: normal;
	font-size: 16px !important;
	text-align: center;
	padding: 0px 200px;
	margin-bottom: 0px !important;
}

.product_form_custom {
	display: none;
}

#ui-datepicker-div {
	z-index: 999 !important;
}

.imgleft .simpletext {
	padding: 60px 0px;
}

#shopify-section-testimonial__bg_title_user_image {
	padding: 0px 0px 0px 0px;
}

#shopify-section-testimonial__bg_title_user_image section {
	padding: 0px;
}

#section_k_testimonial__bg_title_user_image .container>div {
	box-shadow: 3px 3px 9px 1px #ddd;
	padding: 65px 60px 60px 60px !important;
	background: #fff !important;
	border-radius: 10px;
}

#section_k_testimonial__bg_title_user_image .container>div div {
	padding: 0px;
}

#shopify-section-testimonial__bg_title_user_image h4 {
	color: #bd812d;
	margin: 35px 0px 55px 0px;
}

.user_card img {
	width: 150px !important;
	height: 150px !important;
}

.user_address {
	color: #000 !important;
	font-size: 16px !important;
}

.user_data .user_name {
	color: #b54f28 !important;
	font-size: 25px !important;
	font-weight: 500 !important;
}

.user_position {
	color: #c3882b !important;
	text-transform: uppercase !important;
	font-weight: bold !important;
}

.user_data {
	margin-left: 30px !important;
	text-align: left !important;
}

#section_k_testimonial__bg_title_user_image .container>div {
	box-shadow: 3px 3px 9px 1px #ddd;
	padding: 65px 60px 60px 60px !important;
	background: #fff !important;
}

.user_card {
	display: flex;
	justify-content: center;
	align-items: center;
}

.user_data .user_name {
	color: #b54f28 !important;
	font-size: 25px !important;
	font-weight: 500 !important;
	margin: 0px !important;
}

.user_position {
	color: #c3882b !important;
	text-transform: uppercase !important;
	font-weight: bold !important;
	margin: 0px !important;
	padding-bottom: 0px;
}

#shopify-section-testimonial__bg_title_user_image section {
	padding: 0px;
	background-repeat: no-repeat;
	background-position: calc(100% - -140px) calc(100% - 85px);
	background-size: 416px;
	padding: 0px 0px 115px 0px;
}

.trademark {
	font-weight: 500;
	font-size: 10px;
	position: relative;
	top: -7px;
}

#shopify-section-testimonial__bg_title_user_image .text-center.simpletext h2 span.trademark {
	font-size: 16px;
	position: relative;
	top: 0px;
	font-weight: bold !important;
}
.book_personal_stylist_product_parent .sg-widget {
    display: none !important;
}
.user_card_bg .product-mobile-section.simpletext p {
    margin-bottom: 5px !important;
}
@media (min-width: 1161px) {
	.imgleft .simpletext {
		padding: 40px 0px;
		min-height: 400px;
	}
}

@media (max-width: 1160px) and (min-width: 768px) {
	.container {
		padding: 0px 35px !important;
	}
	.imgleft .simpletext {
		padding: 20px 0px;
		height: 100%;
	}
	.bta-booking-fields {
		width: 100%;
	}
	section.page_title {
		margin-bottom: 100px;
		padding: 35px 0px;
	}
	section.page_title h1 {
		font-size: 40px;
		font-weight: 500;
	}
	.simpletext {
		padding: 20px 0px 0px 0px;
		display: flex;
		justify-content: center;
		flex-direction: column;
	}
	.user_card_bg {
		margin-bottom: 100px;
		height: 315px;
	}
	.SliderLeftImage {
		background-size: cover;
		background-repeat: no-repeat;
		height: 100%;
		height: 450px;
		position: absolute;
		top: -65px;
		border: 10px solid #fff;
		border-radius: 50%;
		width: 450px;
		left: 0px;
		right: 0px;
		margin: 0 auto;
	}
	.imgleft .simpletext p {
		font-size: 14px !important;
	}
	.simpletext p {
		font-size: 14px;
		line-height: 1.4em;
		font-weight: 300;
		width: 100%;
	}
	.simpletext h5 {
		font-size: 16px;
	}
	.simpletext h4 {
		padding-bottom: 5px;
		font-size: 28px;
	}
	.rondedImageLeft .slick-slide {
		height: 320px;
		width: 100% !important;
	}
	.rondedImageLeft.imgLeft {
		padding: 0px;
	}
	.imgleft {
		padding-right: 0px;
	}
	.book_personal_stylist_product_parent p {
		font-size: 16px !important;
		width: 700px;
	}
	h3 {
		font-size: 32px!important;
		padding-bottom: 20px!important;
	}
	.book_personal_stylist_product_parent form {
		padding: 50px 20px 55px 20px;
	}
	.book_personal_stylist_product_parent .bta-booking-field:last-child label {
		font-size: 15px !important;
		padding: 0px;
		width: 631px;
	}
	.stylistRow {
		padding-bottom: 60px !important;
	}
	section.wuscrf.bgwhite {
		padding-top: 0px;
	}
	body h3 {
		font-size: 32px !important;
	}
	.book_personal_stylist_product_parent .bta-booking-field:last-child {
		text-align: center;
	}
	#shopify-section-testimonial__bg_title_user_image section {
		padding: 0px 0px 100px 0px;
	}
	#shopify-section-testimonial__bg_title_user_image section {
		background-position: calc(100% - -140px) calc(100% - 75px);
		background-size: 350px;
		padding: 0px 0px 100px 0px;
	}
}

@media (max-width: 991px) and (min-width: 768px) {
	.imgleft .simpletext p {
		font-size: 13px !important;
	}
	.SliderLeftImage {
		background-size: cover;
		background-repeat: no-repeat;
		height: 100%;
		height: 360px;
		position: absolute;
		top: -25px;
		border: 10px solid #fff;
		border-radius: 50%;
		width: 365px;
		left: 0px;
		right: 0px;
		margin: 0 auto;
	}
}

@media (max-width:767px) {
	.book_personal_stylist_product_parent p {
		text-align: center !important;
	}
	section p {
		font-size: 13px !important;
		line-height: 1.5em!important;
		color: #000000 !important;
		font-weight: 400 !important;
		margin-bottom: 20px !important;
		padding-top: 0px !important;
	}
	body h3.book_an_app_h1 {
		margin-bottom: 15px;
		font-size: 28px !important;
	}
	.container {
		width: 100% !important;
		max-width: 100% !important;
		padding: 0px 25px !important;
	}
	section.page_title {
		margin-bottom: 100px;
	}
	.page_title h1 {
		font-size: 23px !important;
		line-height: 23px;
		color: #fcf9f4;
	}
	.page_title {
		padding: 13.5px 0px !important;
	}
	.wrap-dropdown-content.prodesc {
		display: none;
	}
	.btns-cartBuy-inner a {
		display: none;
	}
	.rondedImageLeft.imgLeft {
		display: flex;
		padding: 0px;
		justify-content: center;
		margin-top: -100px;
		margin-bottom: 20px;
	}
	.SliderLeftImage {
		height: 200px;
		position: relative;
		top: 0px !important;
		left: unset !important;
		right: 0px !important;
		border: 5px solid #fff;
		width: 200px;
	}
	.rondedImageLeft .slick-slide {
		height: 200px;
		width: 100% !important;
	}
	.product-mobile-section.simpletext {
		height: auto;
		text-align: center;
		width: 100%;
		padding: 0px;
		margin: 0px 0px 15px 0px !important;
	}
	.stylistRow h5 {
		font-size: 16px;
	}
	.stylistRow h4 {
		font-size: 26px !important;
		font-weight: 400 !important;
		margin-top: 5px;
		margin-bottom: 5px;
	}
	.stylistRow p {
		font-size: 13px !important;
		width: 375px !important;
		margin: 0 auto;
		max-width: 100%;
		text-align: left;
	}
	.user_card_bg {
		margin-bottom: 0px;
	}
	.book_personal_stylist_product_parent {
		padding-top: 55px;
	}
	.sec-title {
		text-align: center;
	}
	.form-group i {
		bottom: 4px;
	}
	.book_personal_stylist_product_parent p {
		font-size: 10pt !important;
		line-height: 17pt!important;
		width: 100%;
	}
	.top_background {
		display: none;
	}
	.book_personal_stylist_product_parent form {
		padding: 25px 0px;
	}
	.booking-form>div:nth-child(1),
	.booking-form>div:nth-child(2) {
		width: 100%;
		float: left;
	}
	label[for="bta-field-contact_information"] {
		font-size: 18px !important;
		margin-bottom: 0px !important;
	}
	.booking-form>div {
		padding: 0px 20px;
		margin-bottom: 15px;
	}
	.book_personal_stylist_product_parent form label:not(.red_label),
	.book_personal_stylist_product_parent form label:not(.error) {
		font-size: 15px;
	}
	.book_personal_stylist_product_parent form .form-group input,
	.book_personal_stylist_product_parent form textarea,
	.book_personal_stylist_product_parent form select {
		font-size: 14px !important;
		padding: 10px 5px 8px 5px !important;
	}
	.booking-form .bta-booking-fields {
		margin-bottom: 0px;
		margin-top: 25px;
	}
	.bta-booking-fields legend {
		font-size: 18px;
		padding-top: 0px !important;
	}
	.book_personal_stylist_product_parent form .bta-booking-field fieldset label {
		font-size: 15px;
	}
	.book_personal_stylist_product_parent .bta-booking-field:last-child label {
		font-size: 14px !important;
		padding: 0px 0px;
		margin: 0px -10px;
	}
	.btns-cartBuy-inner a {
		display: none !important;
	}
	.btns-cartBuy-inner button {
		background: #b65327 !important;
		font-size: 18px !important;
		height: auto !important;
		width: auto !important;
		border-radius: 10px !important;
		text-align: center !important;
		margin: 10px auto 0px auto !important;
		font-weight: 500 !important;
		padding: 11px !important;
		color: #fff !important;
	}
	section.wuscrf {
		padding-top: 35px !important;
		padding-bottom: 0px !important;
	}
	.stylistRow {
		padding-bottom: 0px !important;
	}
	.care_row .care_img img {
		width: 22px;
		height: auto;
	}
	.care_row {
		margin-bottom: 20px;
	}
	.care_row .care_img {
		width: 50px;
		height: 50px;
	}
	.care_text p {
		font-size: 14px;
	}
	.care_row .care_img img {
		width: 22px;
		height: auto;
	}
	footer#footer {
		text-align: center;
		margin-top: 0px;
	}
	.bottom_background {
    background-position: calc(100% - -140px) bottom;
    right: 0px;
    width: 600px;
    height: 600px;
    position: absolute;
    bottom: -68px;
    max-width: 100%;
    background-size: 275px;
}
	.client-mobile.client-desktop {
		background-size: cover;
		height: 100%;
		margin-bottom: 0px;
		margin-top: 0px;
		padding: 35px 0px 50px 0px;
	}
	.checkmark {
		position: absolute;
		background-color: #fff;
		border-radius: 50%;
		width: 20px;
		height: 20px;
		margin-left: 1px;
		margin-top: 1px;
		left: 0px;
	}
	.bta-booking-fields input:checked~.checkmark {
		background-color: #C3872B;
		border-radius: 50%;
		width: 19px;
		height: 19px;
		margin-left: 2px;
		left: 0px;
		margin-top: 2px;
	}
	#section_k_slider_meet_out_stylist .container .imgRight,
	#section_k_slider_meet_out_stylist .container .imgleft {
		margin: 0px 0px;
	}
	.book_personal_stylist_product_parent form textarea {
		height: 115px;
	}
	.book_personal_stylist_product_parent form textarea#bta-field-comments_optional {
		height: 115px;
	}
	#shopify-section-testimonial__bg_title_user_image {
		padding: 0px 0px 0px 0px;
	}
	#shopify-section-testimonial__bg_title_user_image section {
		padding: 0px;
	}
	#section_k_testimonial__bg_title_user_image .container>div {
		box-shadow: 3px 3px 9px 1px #ddd;
		padding: 65px 60px 60px 60px !important;
		background: #fff !important;
		border-radius: 10px;
	}
	#section_k_testimonial__bg_title_user_image .container>div div {
		padding: 0px;
	}
	#shopify-section-testimonial__bg_title_user_image h4 {
		color: #bd812d;
		margin: 35px 0px 55px 0px;
	}
	.user_card img {
		width: 150px !important;
		height: 150px !important;
	}
	.user_address {
		color: #000 !important;
		font-size: 16px !important;
	}
	.user_data .user_name {
		color: #b54f28 !important;
		font-size: 25px !important;
		font-weight: 500 !important;
	}
	.user_position {
		color: #c3882b !important;
		text-transform: uppercase !important;
		font-weight: bold !important;
	}
	.user_data {
		margin-left: 30px !important;
		text-align: left !important;
	}
	#section_k_testimonial__bg_title_user_image .container>div {
		box-shadow: 3px 3px 9px 1px #ddd;
		padding: 65px 60px 60px 60px !important;
		background: #fff !important;
	}
	.user_card {
		display: flex;
		justify-content: center;
		align-items: center;
	}
	.user_data .user_name {
		color: #b54f28 !important;
		font-size: 25px !important;
		font-weight: 500 !important;
		margin: 0px !important;
	}
	.user_position {
		color: #c3882b !important;
		text-transform: uppercase !important;
		font-weight: bold !important;
		margin: 0px !important;
		padding-bottom: 0px;
	}
	#shopify-section-testimonial__bg_title_user_image section {
		padding: 0px;
		background-repeat: no-repeat;
		background-position: calc(100% - -185px) bottom;
		background-size: 475px;
		background-image: none !important;
	}
	#shopify-section-testimonial__bg_title_user_image {
		padding: 60px 0px 60px 0px;
	}
	#section_k_testimonial__bg_title_user_image .container>div {
		box-shadow: 3px 3px 9px 1px #ddd;
		padding: 40px 25px 40px 25px !important;
		background: #fff !important;
		border-radius: 10px;
	}
	.user_card img {
		width: 75px !important;
		height: 75px !important;
	}
	.user_data .user_name {
		font-size: 18pt !important;
		margin: 0px 0px 8px 0px !important;
		line-height: 18pt;
	}
	.user_position {
		font-size: 10pt !important;
		line-height: 1.2em !important;
	}
	.user_address {
		font-size: 10pt !important;
		line-height: 1.3em !important;
	}
	.user_data {
		margin-left: 12px !important;
		text-align: left !important;
	}
	#shopify-section-testimonial__bg_title_user_image h4 {
		color: #bd812d;
		margin: 20px 0px 30px 0px;
		font-size: 12pt;
	}
	#shopify-section-testimonial__bg_title_user_image section {
		background-image: none !important;
	}
	#shopify-section-testimonial__bg_title_user_image .text-center.simpletext h2 span.trademark {
		font-size: 14px !important;
	}
	.trademark {
		font-size: 9px !important;
	}
	.bottomBg {
		background-size: 250px;
		position: absolute;
		top: -25px;
		width: 300px;
		right: 00px;
		height: 350px;
		background-position: 50px 0px;
		z-index: -1;
	}
  	.book_personal_stylist_product_form .form-group {
        width: 100%;
    }
  	.book_personal_stylist_product_parent form select {
    height: 45px !important;
    margin: 0px !important;
}
.form-group.sesami__buttonWrapper button {
    
    height: 45px;
    margin-top: 0px;
    border-radius: 5px !important;
    font-size: 14px !important;
    padding: 0px 5px;
}
}

@media (max-width: 358px) {
	.user_position {
		font-size: 9pt !important;
		line-height: 1.2em !important;
	}
	.user_address {
		font-size: 9pt !important;
		line-height: 1.3em !important;
	}
}
</style>
<!-- Product Detail -->
<section class="page_title p-t-40 p-b-50 flex-col-c-m">
  <h1 class="snize-search-results-page-title l-text2 t-center">Book Personal Stylist</h1>
</section>
<div class="stylistRow bgwhite p-t-35 p-b-80">
  	<div class="flex-w flex-sb">
      	<div class="user_card_bg desktop mobile">
          	<div class="container">
              	<div class="row">
                 	<div class="col-md-6 rondedImageLeft imgLeft respon5">
        				<div class="wrap-slick3 flex-sb flex-w">
							{% comment %}
                          <div class="wrap-slick3-dots hide_mobile">
                            {% assign img_cnt = 1 %}
                              {% for image in product.images %}
                              {% assign img_cnt = forloop.index %}

                             <div>{% include 'responsive-image' with
                                  image: image.src ,
                                  max_width: 90,
                                  max_height: 90
                                %}
                             </div>
                              {% endfor %}
                          </div>
                          
                          {% endcomment %}

                          <div id="product-image-carousel">           
                            {% for image in product.images %}

                            {% assign datavariant = '' %}{% assign datavariantTitle = '' %}
                            {% assign variant = image.variants.first%}
                            {% if variant %} {% assign datavariant = variant.id %} {% assign datavariantTitle = variant.title %}{%endif%}
                            <div class="item-slick3" data-variant='{{datavariant}}' data-variantitle='{{datavariantTitle}}'>
                             
                              <div class="wrap-pic-w SliderLeftImage">
                                {% include 'responsive-image' with
                                  image: image.src ,
                                  max_width: 501,
                                  max_height: 500
                                %}

                              </div>
                            </div>
                            {% endfor %}
                          </div>   

                       </div> 
    				</div> 

                  <div class="col-md-6 imgleft respon5 product-mobile-section-wrap">

          <div class="product-mobile-section simpletext">
              <h5>Meet</h5>
              <h4 class="product-detail-name m-text16 p-b-13">
                {{ product.title  | remove: "Book Personal Stylist" }}
              </h4>

              <p class="s-text8">
              {{ product.description | split:"[video]" | first }}
              </p>
			{% comment %}
            <div class="product-top-wrap">
              <div class="product-top-left">
                  {% assign variant = product.selected_or_first_available_variant %}
                    <p class="product_prc">
                        {% if product.selected_or_first_available_variant.price < product.selected_or_first_available_variant.compare_at_price %}

                                <span id="comparePrice" class="m-text17a p-r-10 money prodmoney">
                                  {% if settings.hide_cents %}
                                  {{ product.selected_or_first_available_variant.compare_at_price | money | remove: '.00' }}
                                   {% else %} 
                                 {{ product.selected_or_first_available_variant.compare_at_price | money }}
                                {% endif %}
                                </span>
                                <span id='productPrice' class="m-text17b money prodmoney">
                                  {% if settings.hide_cents %}
                                  {{ product.selected_or_first_available_variant.price | money | remove: '.00' }}
                                   {% else %} 
                                 {{ product.selected_or_first_available_variant.price | money }}
                                {% endif %}
                                </span>
                                <span id='salePrice' class="sale_prc money prodmoney">
                                  {% assign compare_price = variant.compare_at_price  %}
                                      {% assign product_price = variant.price %}
                                  {% include 'price-sale'  %}
                                </span>

                        {% else %}
                              <span id='productPrice'  class="m-text17 money prodmoney">
                                {% if settings.hide_cents %}
                                {{ product.selected_or_first_available_variant.price | money | remove: '.00' }}
                                 {% else %} 
                              {{ product.selected_or_first_available_variant.price | money }}
                              {% endif %}
                              </span>
                        {% endif %}
                        </p>

                  <a href="#looxReviews"><div class="loox-rating" data-id="{{ product.id }}" data-rating="{{ product.metafields.loox.avg_rating }}" data-raters="{{ product.metafields.loox.num_reviews }}"></div></a>
                       {% comment %} 
                        <p class="s-text8 p-t-10">
                          {{ product.content | split:"[video]" | first | strip_html | truncatewords:20 }}.
                        </p>
                       {% endcomment %}
                        {% assign chk_color = false %}
                        {% assign ind = 0 %}
                  {% for opt in product.options_with_values %}  
                        {% assign downcased_opt =  opt.name | downcase %}
                      {% if downcased_opt contains 'color' or downcased_opt contains 'colour'  or downcased_opt contains 'amount' %}
                        {% assign chk_color = true %}
                          {% assign ind = product.variants.size %}
                      {% endif %}
                  {% endfor %}
              </div>
              <div class="product-top-right">
                {% unless current_variant.available %}

                 {% else %}
                    {% if current_variant.inventory_quantity > 25 %}
                          <div id="variantQuantity" class="in-stock">In Stock.</div>
                    {% endif %}

                    {% if current_variant.inventory_quantity < 25 %}

                    {% endif %}

               {% endunless %}
              </div>
            </div>
            {% endcomment %}


            {% include 'limited_prod' %}
                  <!--  -->
                  <div class="p-t-10 p-b-60 product_form_custom">

                    {% if settings.product_people_viewing_enable %}        
                     {% include 'product-view-visitors' %}
                    {% endif %}
                  </div>
          </div>




        <!--  -->
       <div class="wrap-dropdown-content active-dropdown-content prodesc">


         {% if settings.show_duplicates_buy_buttons %}
        <div class="btns-cartBuy">
            <div class="btns-cartBuy-inner">
                <button name="add" id="button-cart" {% unless current_variant.available %}disabled="disabled"{% endunless %}  class="button-atca btnsubmit">
                    {% unless current_variant.available %}
                    Not Available
                    {% else %}
                    Book Stylist
                    {% endunless %}
                </button>

                 <a href="/cart/{{cartlist}}{{current_variant.id}}:1"  class="buy-btn bnowbtn">Buy Now</a>
            </div>
        </div>
        {% endif %}
         <br>

        </div>




      </div>
              	</div>
          </div>
  		</div>
    <div class="book_personal_stylist_product_parent">
      <div class="top_background"></div>
      <div class="container">
      	<h3 class="book_an_app_h1">Book an Appointment</h3>
      	<p class="book_an_app_h1_p">Our stylists are here to consult with you so that you can make the most of your Scarflings purchase. They have a wealth of experience and come from a wide range of fashion backgrounds so you can be sure to connect with someone who understands your individual style.</p>
       
      	<form action="/cart/add" data-productid="{{ product.id }}" method="post" enctype="multipart/form-data" id="form_buy" class="book_personal_stylist_product_form">
<!-- Begin ReCharge code -->
{% if product.available %}{% include 'subscription-product' %}{% endif %}
<!-- End ReCharge code -->

                    <div class="form-group p-b-10"  {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}style="display:none"{% endif %}>
                      <select name="id" data-productid="{{ product.id }}" id="productSelect" class="form-control">
                        {% for variant in product.variants %}
                        {% if variant.available %}
                        
                        <option {% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %} data-sku="{{ variant.sku }}" value="{{ variant.id }}">{{ variant.title }} - {{ variant.price | money_with_currency }}</option>
                        {% else %}
                        <option disabled="disabled">
                          {{ variant.title }} - {{ 'products.product.sold_out' | t }}
                        </option>
                        {% endif %}
                        {% endfor %}
                      </select>
                      {% if product.available and product.variants.size > 1 %}
                      {% for option in product.options %}
                      {% include 'swatch' with option %}
                      {% endfor %}
                      {% endif %}
                    </div>
                    <div class="form-group">
                      {% if settings.product_quantity_enable %}
                      <div class="flex-r-m flex-w p-t-12">
                        <div class="w-size160 flex-m flex-w">
                          {% if product.available %}
                          <div class="flex-w bo5 of-hidden m-r-22 m-t-10 m-b-10 qty_pro">
                            <button class="btn-num-product-down color1 flex-c-m size7 bg8 eff2">
                              <i class="fs-12 fa fa-minus" aria-hidden="true"></i>
                            </button>
                            <input class="size8 m-text18 t-center num-product" type="number" id="Quantity" name="quantity" value="1">
                            <button class="btn-num-product-up color1 flex-c-m size7 bg8 eff2">
                              <i class="fs-12 fa fa-plus" aria-hidden="true"></i>
                            </button>
                          </div>
                          {% endif %}                
                        </div>             
                      </div>
                      {% endif %}
                      
                      {% include "new-booking-form" %}
                      
                      <div class="for-desktop">
                        <div class="btn-addcart-product-detail size9 m-t-10 m-b-10">
                          <!-- Button -->
                          <div id="xmain_btn">
                            <button name="add" id="xbutton-cart" {% unless current_variant.available %}disabled="disabled"{% endunless %} class="flex-c-m sizefull button-atca bg1 bo-rad-23 hov1 s-text1 buynow_btn btnsubmit" >
                              {% unless current_variant.available %}
                              Not Available
                              {% else %}
                              Book Stylist
                              {% endunless %}
                            </button>
                          </div>              
                        </div>            
                      </div>
                      
                      
                      <div class="btns-cartBuy">
                        <div class="btns-cartBuy-inner">
                          <button  name="add" id="button-cart" {% unless current_variant.available %}disabled="disabled"{% endunless %} class="button-atca btnsubmit">
                            {% unless current_variant.available %}
                            Not Available
                            {% else %}
                            Book Stylist
                            {% endunless %}
                          </button>
                          
                          
                          <a href="/cart/{{cartlist}}{{current_variant.id}}:1"  class="buy-btn bnowbtn">Buy Now</a>
                        </div>
                      </div>
                      
                      
                      
                    </div>
                  </form>
      </div>
      <div class="bottom_background"></div>
    </div>
  </div>
</div>

{% include 'basb_c_f_y_scarf' %}

{% section 'wys-new' %}

{% section 'client-section' %}

{% section 'testimonial__bg_title_user_image' %}

{% section 'welcome-cta-section' %}



{{ 'option_selection.js' | shopify_asset_url | script_tag }}

<script>

  // Pre-loading product images, to avoid a lag when a thumbnail is clicked, or
  // when a variant is selected that has a variant image.
  Shopify.Image.preload({{ product.images | json }}, '{{ product_img_size }}');
  var optionSel = null;
  var selectCallback = function( variant, selector ) {
    var $addToCart = $('.button-atca'),
        $productPrice = $('#productPrice'),
        $comparePrice = $('#comparePrice'),
        $salePrice = $('#salePrice'),
        $variantQuantity = $('#variantQuantity'),
        $quantityElements = $('.quantity-selector, label + .js-qty'),
        $addToCartText = $('#addToCartText'),
        $featuredImage = $('#productPhotoImg');
        $buyNowBtn = $('.bnowbtn');
    if (variant) {
      // Update variant image, if one is set
      // Call timber.switchImage function in shop.js
      {% if chk_color %}
      if (variant.featured_image) {
          $('#product-image-carousel').slick( 'slickGoTo', variant.featured_image.position - 1 );
      }
      {% endif %}

      // Select a valid variant if available
      if (variant.available) {
        //update buy button url
        $buyNowBtn.attr('href', '/cart/{{cartlist}}'+ variant.id +':1');
        // We have a valid product variant, so enable the submit button
        $addToCart.removeClass('disabled').prop('disabled', false);
        $buyNowBtn.show();
        $addToCart.html("Book Stylist");
        $quantityElements.show();

        // Show how many items are left, if below 10
        {% if settings.product_quantity_message %}
        if (variant.inventory_management) {
          if (variant.inventory_quantity < 25) {
            $variantQuantity.html({{ 'products.product.only_left' | t: count: '1' | json }}.replace('1', variant.inventory_quantity)).addClass('less-in-stock').removeClass('in-stock');
          } else {
            $variantQuantity.html('In Stock.').addClass('in-stock').removeClass('less-in-stock');
          }
        }
        else {
          $variantQuantity.removeClass('is-visible');
        }
        {% endif %}

      } else {
        // Variant is sold out, disable the submit button
        $addToCart.addClass('disabled').prop('disabled', true);
        $buyNowBtn.hide();
        $addToCart.html({{ 'products.product.sold_out' | t | json }});
        $variantQuantity.removeClass('is-visible');
        if (variant.incoming) {
          $variantQuantity.html({{ 'products.product.will_be_in_stock_after' | t: date: '[date]' | json }}.replace('[date]', variant.next_incoming_date)).addClass('is-visible');
        }
        else {
          $variantQuantity.html({{ 'products.product.out_stock' | t  | json }}).addClass('less-in-stock').removeClass('in-stock');
        }
        $quantityElements.hide();
      }

      // Regardless of stock, update the product price
      Shopify.money_format = '{{shop.money_format}}'; 
      if(Shopify.money_format.indexOf('decimals')==-1){ Shopify.money_format = '{{shop.money_format}}'.replace('amount','amount_no_decimals'); }
      

      //var customPrice = timber.formatMoney( Shopify.formatMoney(variant.price,Shopify.money_format) );
      var a11yPrice = Shopify.formatMoney(variant.price, Shopify.money_format);
      // var customPriceFormat = ' <span aria-hidden="true">' + customPrice + '</span>';
	  $productPrice.html(a11yPrice);
      {% if settings.product_show_compare_at_price %}
      if (variant.compare_at_price > variant.price) {
        //var comparePrice = timber.formatMoney(Shopify.formatMoney(variant.compare_at_price, Shopify.money_format));

        var a11yComparePrice = Shopify.formatMoney(variant.compare_at_price, Shopify.money_format);
        //customPriceFormat = ' <span aria-hidden="true">' + customPrice + '</span>';
        //customPriceFormat += ' <span aria-hidden="true"><s>' + comparePrice + '</s></span>';
        //customPriceFormat += ' <span class="visually-hidden old-price">' + a11yComparePrice + '</span>';
        /*customPriceFormat += ' <span class="visually-hidden"><span class="visually-hidden">{{ "products.general.sale_price" | t }}</span> ' + a11yPrice + '</span>';*/
        $comparePrice.html(a11yComparePrice);
        var a11ySalePrice = Shopify.formatMoney(variant.compare_at_price - variant.price, Shopify.money_format);
        $salePrice.html(a11ySalePrice);
      }
      {% endif %}
      

      // Also update and show the product's compare price if necessary
      if ( variant.compare_at_price > variant.price ) {
        // var priceSaving = timber.formatSaleTag( Shopify.formatMoney(variant.compare_at_price - variant.price, '{{ shop.money_format }}') );
        {% comment %}
        priceSaving += ' (' + ( (variant.compare_at_price - variant.price)*100/(variant.compare_at_price) ).toFixed(0) + '%)';
        {% endcomment %}
        //$comparePrice.html({{ 'products.general.save_html' | t: saved_amount: '[$]' | json }}.replace('[$]', priceSaving)).show();
      } else {
        $comparePrice.hide();
      }

      

    } else {
      // The variant doesn't exist, disable submit button.
      // This may be an error or notice that a specific variant is not available.
      $addToCart.addClass('disabled').prop('disabled', true);
      $addToCartText.html("Not Available");
      $variantQuantity.removeClass('is-visible');
      $quantityElements.hide();
    }
    if (variant && variant.featured_image) {
      var originalImage = $(".thumbnails img");
      var newImage = variant.featured_image;
      var element = originalImage[0];
      Shopify.Image.switchImage(newImage, element, function (newImageSizedSrc, newImage, element) {
        $(element).parents('a').attr('href', newImageSizedSrc);
        $(element).attr('src', newImageSizedSrc);
        $('.thumbnails .zoomWindowContainer div').css('background-image','url('+newImageSizedSrc+')');
      });
    }
    // BEGIN SWATCHES
    if (variant) {
      var form = $('#' + selector.domIdPrefix).closest('form');
      for (var i=0,length=variant.options.length; i<length; i++) {
        var radioButton = form.find('.swatch[data-option-index="' + i + '"] :radio[value="' + variant.options[i] +'"]');
        if (radioButton.length) {
          radioButton.get(0).checked = true;
        }
      }
    }
    // END SWATCHES
       if (BOLD && BOLD.recurring_orders && BOLD.recurring_orders.app && BOLD.recurring_orders.app.ee &&
          typeof BOLD.recurring_orders.app.ee.emit === 'function'){
        BOLD.recurring_orders.app.ee.emit('variant_changed');
      }
  };

  jQuery(function($) {
    var product = {{ product | json }};
    {% if settings.product_quantity_message %}
    {% for variant in product.variants %}
    product.variants[{{ forloop.index0}}].incoming = {{ variant.incoming | default: false }};
    product.variants[{{ forloop.index0}}].next_incoming_date = {{ variant.next_incoming_date | date: format: 'month_day_year' | json }};
    product.variants[{{ forloop.index0}}].inventory_quantity = {{ variant.inventory_quantity }};
    {% endfor %}
    {% endif %}

    optionSel = new Shopify.OptionSelectors('productSelect', {
      product: product,
      onVariantSelected: selectCallback,
      enableHistoryState: false
    });

    // Add label if only one product option and it isn't 'Title'. Could be 'Size'.
    {% if product.options.size == 1 and product.options.first != 'Title' %}
    $('.selector-wrapper:eq(0)').prepend('<label>{{ product.options.first | escape }}</label>');
                                         {% endif %}
                                         /* Hide selectors if we only have 1 variant and its title contains 'Default'. */
                                         {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                                         $('.selector-wrapper').hide();
    {% else %}
    $('#variantBreak').removeClass('hr--clear');
    {% endif %}
    //$('.selector-wrapper').hide();
  }); 
  
   $(document).ready(function() {

        setInterval(function() 
        {
            $("#main_btn .buynow_btn").effect( "bounce", {times:3}, 1500);
        },5000);
		
    });
</script>

{% if product.metafields.loox.num_reviews %}
<script id="looxSchemaJson" type="application/ld+json">
{
	"@context": "http://schema.org",
	"@type": "Product",
	"@id": {{ canonical_url | json }},
	"aggregateRating": {
		"@type": "AggregateRating",
		"ratingValue": "{{ product.metafields.loox.avg_rating }}",
		"reviewCount": "{{ product.metafields.loox.num_reviews }}"
	},
	"name": "{{ product.title | escape }}",
    "brand": "Scarflings",
    "description": "Purveyors of the most fabulous and versatile accessory—the Scarf!",
    "review": "{{ product.metafields.loox.avg_rating }}",
    "sku": "{{ current_variant.sku }}"
}
</script>
{% endif %}


<script>
// setTimeout(function(){ 
   //	$('#product-image-carousel .slick1-dots').hide();
  // console.log(2);
  //}, 10000);
  
  //function to change variant on click of image in slick slider
  function clickSlickCallBack(currentSlide){
    var slick = $('#product-image-carousel').slick('getSlick');
    var curr = slick.$slides[currentSlide];
    if($(curr).data('variant')!='' && $(curr).data('variant')!=undefined && $(curr).data('variant')!=null){
      if(optionSel != null){
	      optionSel.selectVariant($(curr).data('variant'));
      }
      $('.single-option-selector').val($(curr).data('variantitle'));
    }
  }	
  
  
  $('#product-image-carousel').on('afterChange', function(event, slick, currentSlide){   
      	$('#product-image-carousel .slick1-dots').show();
    //console.log(1);
    setTimeout(function(){ 
   	 $('#product-image-carousel .slick1-dots').hide();
  }, 3000);
    clickSlickCallBack(currentSlide);
});
  
 
</script>

<!-- Klaviyo Product Tracking -->
<script text="text/javascript">
  var _learnq = _learnq || [];

  var item = {
    Name: {{ product.title|json }},
    ProductID: {{ product.id|json }},
    Categories: {{ product.collections|map:'title'|json }},
    ImageURL: "https:{{ product.featured_image.src|img_url:'grande' }}",
    URL: "{{ shop.secure_url }}{{ product.url }}",
    Brand: {{ product.vendor|json }},
    Price: {{ product.price|money|json }},
    CompareAtPrice: {{ product.compare_at_price_max|money|json }}
  };

  _learnq.push(['track', 'Viewed Product', item]);
  _learnq.push(['trackViewedItem', {
    Title: item.Name,
    ItemId: item.ProductID,
    Categories: item.Categories,
    ImageUrl: item.ImageURL,
    Url: item.URL,
    Metadata: {
      Brand: item.Brand,
      Price: item.Price,
      CompareAtPrice: item.CompareAtPrice
    }
  }]);
</script>
<!-- End of Klaviyo Product Tracking -->


<!-- Klaviyo Add To Cart (ATC) Tracking -->
<script text="text/javascript">
	var _learnq = _learnq || [];
	document.querySelector (".button-atca").addEventListener('click',function (){
 		_learnq.push(['track', 'Added to Cart', item]);
	});
</script>
<!-- End of Klaviyo Add To Cart (ATC) Tracking -->
<script>
$(document).ready(function () {

	$("p").each(function () {

		if ($.trim($(this).html()).length == 0) {
			$(this).remove();
		}

	});

	var myVar = setInterval(myTimer, 1000);

	function myTimer() {

		if ($('.bta-booking-fields').is(':empty')) {

		} else {
			$(".bta-booking-fields").each(function () {

				//if($.trim($(this).html()).length >= 0) {
				$("input[type='radio']").wrap('<span class="radio_circle"></span>');
				$(".book_personal_stylist_product_parent .bta-booking-field[data-bta-repeating-label='Select an appointment type'] label").addClass("radioLables");

				$("input[type='radio']").after($('<span class="checkmark"></span>'));
				$(".book_personal_stylist_product_parent label:not('.radioLables')").text(function () {
					return $(this).text().slice(0, -1);
				});
				//}
				clearInterval(myVar);

			});
		}

	}


});
  
   $('form[action="/cart/add" data-productid="{{ product.id }}"]').submit(function(t) {
     t.preventDefault();
	 var submit_form = false;
     if($("[name='properties[Date]']").val() == ""){
       $(".sesami__error").text("Please select date and time");
       submit_form = false;
     }else{
       submit_form = true;
     }
     
     if(submit_form){
       $.ajax({
            url: "/cart/add.js",
            type: "post",
            data: $("#form_buy input[type='text'], #form_buy select,#form_buy input[type='number'],#form_buy input[type='hidden']"),
            dataType: "json",
            beforeSend: function() {
                $(".btnsubmit").html("Booking Stylist...")
            },
            complete: function() {
                $(".btnsubmit").html("Book Stylist");
              	$(".btnsubmit").hide();
                $(".sesami__button").hide();
              $("#form_buy").hide();
            },
           success: function(t) {
                  if(window.addToCartButtonRedirectSetting == "1"){
                    window.location.href = "/cart";
                  }
                  else if(window.addToCartButtonRedirectSetting == "2"){

                    window.location.href = "/checkout";

                  }
                  else if(window.addToCartButtonRedirectSetting == "3"){

                    $.getJSON("/cart.js", function(t) {
                      updateAllKarts(t.item_count)
                    }), showKart()

                  } else{

                    $.getJSON("/cart.js", function(t) {
                      updateAllKarts(t.item_count)
                  }), showKart()
                     
                  } 
             
            },
            error: function(t, e, a) {
                alert(t.responseText)
            }
        });
     
     }
     
    });
</script>

{% include 'product-content.zipifypages', contentprt: 'footer' %}